import 'package:flutter/material.dart';

class HealthConfirmScreen extends StatelessWidget {
  const HealthConfirmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Health Confirm'),
      ),
    );
  }
}
